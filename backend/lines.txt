396: '\n'397: '    ext = filename.rsplit(".", 1)[-1].lower()\n'398: '    source_type = ext if ext in ("pdf", "docx", "txt") else "text"\n'399: '\n'400: '    try:\n'401: '        loop = asyncio.get_event_loop()`n        chunks = await loop.run_in_executor(None, pipeline.prepare, filename, content)\n'402: '        chunk_count = await pipeline.store_fast(filename, chunks, user_id, source_type)\n'403: '\n'404: '        # Mark file as needing embedding (progress persisted in DB)\n'405: '        set_indexing_progress(user_id, filename, chunk_count, 0, "indexing")\n'406: '\n'407: '        return {\n'408: '            "filename": filename,\n'409: '            "chunks": chunk_count,\n'410: '            "source_type": source_type,\n'411: '            "status": "done",\n'412: '            "needs_embedding": True,\n'413: '            "message": "File stored, embedding in progress"\n'414: '        }\n'415: '    except Exception as e:\n'416: '        logger.error(f"Process uploaded error for {filename}: {e}\\n{traceback.format_exc()}")\n'417: '        raise HTTPException(status.HTTP_500_INTERNAL_SERVER_ERROR, str(e))\n'418: '\n'419: '\n'420: '@app.post("/documents/embed-batch")\n'421: 'async def embed_batch(body: dict):\n'422: '    """Embed ONE batch of 50 chunks. Called repeatedly by frontend.\n'423: '\n'424: "    This replaces BackgroundTasks which don't work on Vercel serverless.\n"425: '    Each call takes ~1-2 seconds, well within the 5-minute timeout.\n'